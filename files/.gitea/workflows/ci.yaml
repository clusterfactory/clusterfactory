name: ci
on:
  push:
    branches: [main]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: |
          echo "ClusterFactory Gitea Actions running on $(hostname)"
          echo "Repo: ${GITHUB_REPOSITORY:-unset}"
          echo "Ref:  ${GITHUB_REF:-unset}"
      - name: Verify
        run: echo "Workflow OK"
