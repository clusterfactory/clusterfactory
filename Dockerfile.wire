FROM python:3.12-slim

WORKDIR /factory

# Install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy factory code
COPY factory/ factory/

# Set entrypoint
ENTRYPOINT ["python", "-m", "factory"]
CMD ["--platform", "/config/platform.yaml"]
