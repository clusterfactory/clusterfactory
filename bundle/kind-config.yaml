kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: clusterfactory-ci
networking:
  disableDefaultCNI: true  # Calico is installed by CI; kindnet does not enforce NetworkPolicy
  podSubnet: 192.168.0.0/16
nodes:
  - role: control-plane
    labels:
      clusterfactory.io/storage: "true"
