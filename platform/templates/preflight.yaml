{{- if and .Values.workflow.harborAdminPassword (eq (.Values.workflow.harborAdminPassword | default "") "") }}
{{ fail "workflow.harborAdminPassword is required. Set it with --set workflow.harborAdminPassword=..." }}
{{- end }}

{{- if and .Values.gitea.enabled (eq (.Values.gitea.adminPassword | default "") "") }}
{{ fail "gitea.adminPassword is required. Set it with --set gitea.adminPassword=..." }}
{{- end }}

